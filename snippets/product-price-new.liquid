{% if variant.title %} {%- assign price = variant.price -%} {% else %} {%-
assign price = 1999 -%} {% endif %} {%- assign money_price = price | money -%}

<span
	class="text-left block font-bold text-brand-red {{ fontSize }}"
	data-sale-price
>
	{{ money_price }}
</span>
