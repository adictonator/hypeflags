{% if variant.title %} {%- assign price = variant.price -%} {% else %} {%-
assign price = 1999 -%} {% endif %} {%- assign money_price = price | money -%}

<span
	class="price-class text-left justify-start block font-bold text-brand-red {{
		fontSize
	}}"
	data-sale-price
>
	{{ money_price }}
</span>
