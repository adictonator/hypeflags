<div class="hf-modal">
  <div class="hf-modal-body">
    <div class="hf-body-inner">
    	<img src="{{ 'custom-loader-v2.svg' | asset_url }}" alt="loader spinner" />
      	<div>
          <h3>{{ heading }}</h3>
          <p>{{ content }}</p>
      </div>
    </div>
  </div>
</div>

<style>
  .hf-modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    padding: 0 55px;
    background: rgb(0 0 0 / 63%);
    justify-content: center;
    align-items: center;
    z-index: 99999;
  }
  .hf-moda.active {
    display: flex;
  }
  .hf-modal-body {
    display: none;
    justify-content: center;
    align-items: center;
    background: #fff;
    border-radius: 10px;
    max-width: 980px;
    width: 100%;
    max-height: 420px;
    height: 100%;
    margin: 0 auto;
  }
  .hf-modal-body .hf-body-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-direction: column;
    height: 140px;
  }
  .hf-body-inner img {
    width: 40px;
  }
  .hf-body-inner h3 {
    margin-bottom: 10px;
    text-align: center;
    font-weight: 500;
  }
  .hf-body-inner p {
    color: #000;
    margin: 0;
  }
  
  @media(max-width: 767px) {
    .hf-modal {
      padding: 22px;
    }
    .hf-modal-body {
      max-height: 300px;
    }
  } 

</style>